export enum FilterSection {
  HideWatched = 'Hide watched',
  SortBy = 'Sort by',
  Genres = 'Genres',
  Country = 'Country',
  Year = 'Year',
  Cast = 'Cast',
  Rating = 'Rating',
  Title = 'Title',
};

export enum StepEnum {
  Email = 'email',
  Password = 'password'
}